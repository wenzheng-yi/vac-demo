import {
  create,
  NButton,
  NInput,
  NDrawer,
  NDrawerContent,
  NModal,
} from 'naive-ui'

const naive = create({
  components: [NButton, NInput, NDrawer, NDrawerContent, NModal],
})

export default naive
